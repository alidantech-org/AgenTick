# Plan

1. Research open skills, lockfile, watcher, Git-root, and serverless database practices.
2. Build the monorepo and self-contained initializer.
3. Add YAML validation, JSONL/SQLite history, verify, and watch dashboard.
4. Validate tests and package the initial commit.
