# Project Rules

1. Do not delete or replace unrelated user files.
2. Do not write user project state outside `agents/`, except the marked root `.gitignore` block.
3. Do not publish commands that silently claim unsupported behavior.
4. Validate configuration and registry content before use.
5. Keep secrets out of YAML, lock files, logs, and browser bundles.
