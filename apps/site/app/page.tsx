export default function HomePage() {
  return <main><p className="eyebrow">Agent + tick ✅</p><h1 className="hero">Make AI work auditable.</h1><p className="lead">AgenTick watches project changes in real time, validates work against the repository's own <code>agents/</code> specification, and manages portable, integrity-pinned agent skills.</p><section className="grid"><article className="card"><h2>Watch</h2><p>See file events, plan drift, verification results, and findings as work happens.</p></article><article className="card"><h2>Verify</h2><p>Run deterministic project checks and preserve evidence instead of trusting completion claims.</p></article><article className="card"><h2>Skill library</h2><p>Discover open Agent Skills, pin exact versions, verify integrity, and install them into a generated read-only library.</p></article></section></main>;
}
