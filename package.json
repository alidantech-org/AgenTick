{
  "name": "skillib-monorepo",
  "private": true,
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "skillib:install-global": "node scripts/install-skillib-global.mjs"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "prettier": "3.6.2",
    "turbo": "2.10.5",
    "typescript": "7.0.2"
  }
}
